import { styled } from "../../styles/stitches.config";


export const Container = styled('main', {
  maxWidth: 1200,
  margin: '0 auto',
  textAlign: 'center',

  'button': {
    margin: '0 auto',
  }
})
