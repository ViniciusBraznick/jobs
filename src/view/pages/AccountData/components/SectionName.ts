import { styled } from "../../../styles/stitches.config";

export const SectionName = styled('aside', {
  flex: .4,
  paddingTop: '24px',
  display: 'flex',
  flexDirection: 'column',
  gap: '12px'
});
