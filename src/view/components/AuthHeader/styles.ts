import { styled } from "../../styles/stitches.config";

export const Container = styled('header', {
  display: 'flex',
  flexDirection: 'column',
  marginBottom: 64,

  'img':{
    marginBottom: 64
  },

  'p': {
    color: '#9CA3AF'
  }
});
