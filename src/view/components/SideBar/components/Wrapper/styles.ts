import { styled } from "@stitches/react";

export const Container = styled('div', {
  display: "flex",
  flexDirection: "column",

  'a': {
    inset: "initial",
    color: "initial",
    textDecoration: "none",
  }
})
